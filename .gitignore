# SQLite database (regenerated from database/seed.sql)
database/*.db

# Go build / test artifacts
*.exe
*.test
*.out

# Browser automation scratch (Playwright MCP screenshots / snapshots)
.playwright-mcp/
*.png
